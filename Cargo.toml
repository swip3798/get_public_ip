[package]
name = "http_tests"
version = "0.1.0"
authors = ["Christian Schweigel <christian.david.schweigel@sap.com>"]
edition = "2018"

[dependencies]
reqwest = "*"
serde = { version = "1.0", features = ["derive"] }

# serde_json is just for the example, not required in general
serde_json = "1.0"